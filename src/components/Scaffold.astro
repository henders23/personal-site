---
// A learning support that fades once the reader has engaged with the
// section — scaffolding, then fading, enacted. Content remains in the
// DOM and reappears on hover/focus; reduced-motion users see a gentler
// version of the same fade (opacity only, handled in motion.js/CSS).
---
<div class="scaffold" role="note" tabindex="0"><slot /></div>
